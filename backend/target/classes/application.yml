server:
  port: 8080

spring:
  // FIXME: 回家要設定
  datasource:
    url: jdbc:mariadb://localhost:3306/construction_management
    username: root
    password: yourpassword
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql